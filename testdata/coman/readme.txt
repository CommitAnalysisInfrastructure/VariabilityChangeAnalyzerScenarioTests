This directory contains test commits, which were originally used to test the ComAn tool-set. As the
VariabilityChangeAnalyzer together with the core infrastructure and the GitCommitExtractor should pass the same tests
as well, we reuse the respective tests commits here.